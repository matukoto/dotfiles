formatter:
  type: basic
  line_ending: lf
  # 空行を保持する
  retain_line_breaks: true
  # 複数の空行を1つにする
  retain_line_breaks_single: true
  # ドキュメント開始の --- を含める
  include_document_start: true
  # 末尾に改行
  eof_newline: true
